<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginactivation">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Admin_lesson_student_registerActivity"></activity>
        <activity android:name=".Admin_lesson_spisokActivity" />
        <activity android:name=".Admin_teacher_spisokActivity" />
        <activity android:name=".User_page_2" />
        <activity android:name=".User_page_1" />
        <activity android:name=".Student_page_1Activity" />
        <activity android:name=".Admin_SpisokActivity" />
        <activity android:name=".Admin_reg_lessonActivity" />
        <activity android:name=".Admin_reg_teacherActivity" />
        <activity android:name=".Admin_reg_studentsActivity" />
        <activity android:name=".Admin_1Activity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>